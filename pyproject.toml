[tool.poetry]
name = "duelcrypt"
version = "0.0.0"
license = "MIT"
readme = "README.md"
description="bitcoin library in python"
authors = ["Ray Jones"]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
